const {peralta} = require('./function') //object destructuring, envia apenas a função
const peraltajs = require('./function') //enviando de forma normal
console.log(peralta())
console.log(peraltajs.peralta())
